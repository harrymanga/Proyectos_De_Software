# Dependency file for Linux systems (alias de TCP)
# Install: pip install -r MCP_Server/requirements-linux.txt

# MCP SDK - se pinnea <2 para evitar breaking changes (verificado con mcp 1.x)
mcp>=1.0.0,<2

# Note: pywin32 is NOT needed on Linux.
# The TCP relay (ce_tcp_relay.py) is used instead of Windows named pipes.
