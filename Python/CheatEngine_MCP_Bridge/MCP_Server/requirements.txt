# Cheat Engine MCP Bridge - Windows (pipe) - Install: pip install -r MCP_Server/requirements.txt
# Matriz: requirements.txt (Windows pipe) / requirements-tcp.txt (server TCP) /
#         requirements-linux.txt (Linux, alias de TCP). Ver pyproject.toml.

# MCP SDK - se pinnea <2 para evitar breaking changes (verificado con mcp 1.x)
mcp>=1.0.0,<2

# Windows API bindings - Required for Named Pipe communication
pywin32>=306
